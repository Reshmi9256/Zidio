package com.example.zidio;

import com.example.zidio.RestTemlateConfig;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = RestTemlateConfig.class)
public class RestTemlateConfigTests {

    @Test
    void contextLoads() {
        // Ensures RestTemplateConfig loads and beans initialize correctly
    }
}
